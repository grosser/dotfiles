#!/bin/sh
# needs to be an executable so it works with `bundle open` and does not kill rubymine when ctrl+c ing the terminal that opened it
# also verify that calling the same directory twice does not open 2 instances like `open -a` would
# can retry with `export PATH="/Applications/RubyMine.app/Contents/MacOS:$PATH"` if `rubymine .` works
# see https://youtrack.jetbrains.com/issue/RUBY-31628
/Applications/RubyMine.app/Contents/MacOS/rubymine "$@" >/dev/null 2>&1 &
