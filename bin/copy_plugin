#!/usr/bin/env ruby
plugin = ARGV[0] || raise("give me a folder to copy from")
target = "vendor/plugins/#{File.basename plugin}"
`rm -rf #{target}`
`cp -R #{plugin} #{target}`
`rm -rf #{target}/.git`
`rm -rf #{target}/pkg`
`rm -rf #{target}/.bundle`
`rm -rf #{target}/.rvmrc`
