#!/bin/sh
[[ $# -eq 1 ]] || (echo "Usage: kubash <pod_name>" && exit 1)
kubectl exec -it $1 -- /bin/bash -c "stty rows $(tput lines) cols $(tput cols) && export TERM=$TERM && sh -c 'while :; do sleep 29 && printf \"ALIVE\\b\\b\\b\\b\\b\"; done &' && exec bash"
